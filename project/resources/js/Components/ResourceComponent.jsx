import React from 'react';
import styles from '../styles/Resources.module.css'

const ResourceComponent = ({ title, type, description, tags }) => {
    return (
      <div className="ressource-card"></div>
    )
}
export default ResourceComponent;