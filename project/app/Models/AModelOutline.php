<?php 

/*

class ModelName extends Model
{
    // Define the table associated with the model (optional if it follows naming convention)
    // protected $table = 'custom_table_name';
    
    // Define the primary key if it's not 'id' (optional)
    // protected $primaryKey = 'custom_primary_key';

   
    // Enable or disable timestamps (optional)
    // public $timestamps = false;

    // Define which attributes should be mass assignable
    protected $fillable = [
        'column1', 'column2', 'column3', // List of fields that can be mass-assigned
    ];

    // Define which attributes should not be mass assignable (optional)
    // protected $guarded = ['id']; 

    // Specify attributes that should be cast to a different type
    protected $casts = [
        'created_at' => 'datetime',
        'is_active' => 'boolean',
    ];

    // Specify which attributes should be hidden when the model is converted to an array or JSON
    protected $hidden = [
        'password', 'api_token',
    ];

    // Specify which attributes should be visible when the model is converted to an array or JSON (optional)
    // protected $visible = ['name', 'email'];


    // Define relationships (example of a one-to-many relationship)
    // public function posts() {
    //     return $this->hasMany(Post::class);
    // }

    // Example of a many-to-many relationship
    // public function roles() {
    //     return $this->belongsToMany(Role::class);
    // }

    // Example of an inverse relationship
    // public function user() {
    //     return $this->belongsTo(User::class);
    // }

    // Example of a one-to-one relationship
    // public function profile() {
    //     return $this->hasOne(Profile::class);
    // }

   
}
*/